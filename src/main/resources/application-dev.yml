spring:
  datasource:
    url: jdbc:mysql://localhost:3685/qkltest?useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&allowMultiQueries=true
    driver-class-name: com.mysql.jdbc.Driver
    username: qkltest
    password: aba7XWReaxGAGkTj
    platform: mysql
    hikari:
      connection-test-query: SELECT 1 FROM DUAL
      connection-timeout: 600000
      maximum-pool-size: 5000
      max-lifetime: 1800000
      minimum-idle: 20
      validation-timeout: 3000
      idle-timeout: 60000
      connection-init-sql: SET NAMES utf8mb4
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8
    mode: LEGACYHTML5



  mybatis:
    mapper-locations: classpath:com/java/mapper/*.xml

  redis:
    host: 127.0.0.1
    port: 6257
    password: rL2AaJhe4NZtfJbw
    database: 0
    lettuce:
      pool:
        max-active: 1024
        max-wait: -1ms
        max-idle: 8
        min-idle: 1
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 1
    timeout: 5000ms
  cache:
    type: redis

pagehelper:
  helper-dialect: mysql
  reasonable: false
  support-methods-arguments: true
  params: count=countSql

jwt:
  config:
    key: dwewe
server:
  port: 6253